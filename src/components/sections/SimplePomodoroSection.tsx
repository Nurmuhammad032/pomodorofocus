import PomodoroTimer from "../PomodoroTimer";

const SimplePomodoroSection = () => {
  return (
    <section className="min-h-screen relative">
      <PomodoroTimer />
    </section>
  );
};

export default SimplePomodoroSection;
